import { View, Text, StyleSheet } from "react-native";

const RegisterScreen = () => {
  return (
    <View>
      <Text>this is the register view</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default RegisterScreen;
