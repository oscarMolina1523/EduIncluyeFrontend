import { View, StyleSheet, Text } from "react-native";

const HomeScreen = () => {
  return (
    <View>
      <Text>this is the home view</Text>t
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default HomeScreen;
