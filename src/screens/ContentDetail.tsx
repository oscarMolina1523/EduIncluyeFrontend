import { View, Text, StyleSheet } from "react-native";

const ContentDetailScreen = () => {
  return (
    <View>
      <Text>this is the content detail screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default ContentDetailScreen;
